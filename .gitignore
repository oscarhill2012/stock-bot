# Secrets / env
.env
.env.*
!.env.example

# Claude Code local config
.claude/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.eggs/
build/
dist/
pip-wheel-metadata/

# Virtual environments
.venv/
venv/
env/
ENV/
# Also ignore a root-level .venv *symlink* (git worktrees often link to the
# primary checkout's venv); the trailing-slash pattern above matches only a
# real directory, so the symlink form needs this anchored, slash-less entry.
/.venv

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
StockBot.code-workspace

# OS
.DS_Store
Thumbs.db
desktop.ini
bash.exe.stackdump

# Project-specific
graphify-out/
.graphifyignore
.graphify_*
docs/performance/*.csv
docs/performance/*.parquet
logs/
*.log
docs/papers 

# Data caches (raw market data shouldn't be committed)
data/cache/
data/raw/

# LLM report cache — hash-memoised verdicts written by the News + Fundamental
# analysts. Never commit; each developer's cache is an isolated local artefact.
cache/
# Also ignore a root-level cache *symlink* (git worktrees often link to the
# primary checkout's cache); the trailing-slash pattern above matches only a
# real directory, so the symlink form needs this anchored, slash-less entry.
# Root-anchored so it cannot shadow the tracked nested cache packages below.
/cache
# Exception: the backtest cache *source package* must be tracked.
!src/backtest/cache/
# Exception: unit tests for the cache module must also be tracked.
!tests/unit/backtest/cache/

# Local dev SQLite — persistence tests write into it on every run; should
# not be tracked. Schema is owned by SQLAlchemy ORM in orchestrator/persistence.py.
data/stockbot.db
data/stockbot.db-journal
data/stockbot.db-wal
data/stockbot.db-shm

# Surface trace outputs — written by scripts/trace_tick.py; never commit.
docs/surface-traces/

# Local-only worktrees for parallel feature work.
.worktrees/

# Backtest golden-cache SQLite files — binary, large, developer-local only.
backtests/

# Local investigation scripts (untracked)
scratch/
