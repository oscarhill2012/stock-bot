# === Stock data providers ===
finnhub-python>=2.4.20          # get_stock_news, get_social_sentiment
yfinance>=0.2.40                # get_stock_stats (OHLCV + fundamentals)
edgartools>=3.0                 # get_insider_trades, get_company_filings — free EDGAR access
requests>=2.31.0                # Quiver Quant API + general HTTP

# === Agent orchestration ===
google-adk>=0.2.0               # multi-agent buy/sell decision pipeline

# === Data handling ===
pandas>=2.2.0
numpy>=1.26.0
pydantic>=2.6.0                 # typed models for every data source
pydantic-settings>=2.2.0        # load .env into typed config

# === Baselines (SPY benchmark + PyTorch MLP) ===
torch>=2.2.0                    # MLP baseline network
scikit-learn>=1.4.0             # scalers, metrics, train/test split
ta>=0.11.0                      # technical indicators (RSI, MACD, etc.) for MLP features

# === Backtesting + reporting ===
matplotlib>=3.8.0
plotly>=5.20.0

# === Resilience / async ===
tenacity>=8.2.0                 # retry flaky API calls
httpx>=0.27.0                   # async HTTP for parallel data pulls

# === Configuration ===
python-dotenv>=1.0.0

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
